import { Product } from '@/sanity.types'
import React from 'react'

export const AddToWishListButton = ({product,className}:{
  product:Product,
  className?:string
}) => {
  return (
    <div>AddToWishListButton</div>
  )
}
